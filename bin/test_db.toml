[tasks.setup]
script = ["diesel database reset"]

[tasks.test]
command = "cargo"
args = ["test", "--features", "database"]

[tasks.teardown]
script = ["diesel database drop"]

[tasks.run_tests]
dependencies = [
    "setup",
    "test",
    "teardown",
]
